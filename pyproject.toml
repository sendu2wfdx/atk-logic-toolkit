[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "atk-logic-toolkit"
version = "0.4.1"
description = "CLI and Codex skill for MCU waveform analysis with ALIENTEK logic analyzers"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "GPL-3.0-or-later"}
authors = [{name = "sendu2wfdx"}]
keywords = ["alientek", "atk-logic", "dl16", "logic-analyzer", "mcu", "uart", "i2c", "spi"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
  "Programming Language :: Python :: 3",
  "Topic :: System :: Hardware",
]

[project.optional-dependencies]
usb = ["pyusb>=1.2", "libusb-package>=1.0.30; sys_platform == 'win32'"]
dev = ["pytest>=8"]

[project.scripts]
atk-logic = "atk_logic_toolkit.cli:entrypoint"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
